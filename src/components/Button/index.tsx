import BaseButton from "../BaseButton";

export default function Button() {
  return (
    <BaseButton textVariant="body2">
      Botão Master Botão que parece botão
    </BaseButton>
  );
}

Button.Base = BaseButton;
